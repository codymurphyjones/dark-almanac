import LoadingComponentClient from "./LoadingComponentClient";

export default function LoadingComponent() {
  return (
    <main className="flex flex-1 bg-gray-800 h-full">
      <LoadingComponentClient />
    </main>
  );
}
